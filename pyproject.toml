[project]
name = "leetgpu-local"
version = "0.2.0"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
judge = [
    "numpy>=2.0",
    "nvidia-cutlass-dsl[cu13]==4.6.1",
    "tilelang",
    "torch>=2.8",
]
